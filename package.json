{
  "type": "module",
  "name": "my-wedding-ticket",
  "version": "1.0.0",
  "description": "Wedding e-ticket invitation",
  "scripts": {
    "serve": "npx serve . -l 3000",
    "start": "npx serve . -l 3000",
    "build": "node scripts/generate-guests.js",
    "build:guests": "node scripts/generate-guests.js"
  },
  "dependencies": {
    "googleapis": "^144.0.0",
    "@vercel/functions": "^3.0.0"
  }
}
